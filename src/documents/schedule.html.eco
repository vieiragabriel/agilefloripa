---
layout: internal
---

<% for section in @sections_schedule : %>
  <!-- <%= section.toUpperCase() %> -->

  <section class="<%= section %>" id="<%= section %>">
    <%- @partial("section/#{section}.html.eco", @) %>
  </section>

  <!-- / <%= section.toUpperCase() %> -->
<% end %>